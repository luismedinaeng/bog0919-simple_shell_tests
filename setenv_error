Task: 12. setenv, unsetenv

Command to test:
$ setenv PATH= 123

Or any other VARIABLE with a '=' character

Expected output:
VARIABLE is NULL, points to a string of length 0, or contains an '=' character.

Special Conditions:
unsetenv must behave the same.
